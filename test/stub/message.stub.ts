import { MessageSetting } from 'src/entities';

export const messageStub = {
  tsunami: Object.freeze({
    title: 'Test tsunami tittle',
    body: 'Test tsunami body',
    disaster_type: 'TSUNAMI',
  }),

  messageSetting: Object.freeze({
    title: 'Test tsunami tittle',
    body: 'Test tsunami body',
    disaster_type: 'TSUNAMI',
  } as MessageSetting),
};
